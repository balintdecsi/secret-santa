[tool.poetry]
name = "secret-santa"
description = "Chat bot interface with a secret in the LLM."
authors = ["balintdecsi <balint.decsi@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^2.2.3"
google-cloud-aiplatform = "^1.91.0"
google-auth = "^2.39.0"
flask = "^3.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
